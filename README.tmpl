<!-- 
Autogenerated by Typical-Go from template '{{.TemplateFile}}'. 
DO NOT EDIT THIS FILE!
-->

# {{.Title}}

{{.Description}}

{{if .Usages}}### Usage

| Usage | Description |
|---|---|{{range .Usages}}
|`{{.Usage}}`|{{.Description}}|{{end}}
{{end}}

{{if .Configs}}### Configuration

| Name | Type | Default | Required |
|---|---|---|:---:|{{range .Configs}}
|{{.Name}}|{{.Type}}|{{.Default}}|{{.Required}}|{{end}}
{{end}}
----

## Development Guide

### Prerequisite

Install [Go](https://golang.org/doc/install) (It is recommend to install via [Homebrew](https://brew.sh/) `brew install go`)

### Quick Start

```bash
# equivalent with `docker-compose up -d` (if infrastructure not up)
./typicalw docker up 

# drop, create and migrate postgres database (if database not ready)
./typicalw pg reset 

# generate readme (if there is readme update)
./typicalw readme 

# generate mock (if require mock)
./typicalw mock 

# run test 
./typicalw test

# run the application
./typicalw run 

# release the distribution
./typicalw release 
```

{{if .BuildUsages}}### Commands
| Command | Description |
|---|---|{{range .BuildUsages}}
|`{{.Usage}}`|{{.Description}}|{{end}}
{{end}}